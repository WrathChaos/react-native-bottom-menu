export function _textStyle(textColor, fontSize) {
  return {
    fontSize,
    color: textColor
  };
}

export default {
  container: {
    padding: 8,
    marginLeft: 3,
    marginRight: 3,
    alignItems: "center",
    alignContent: "center",
    flexDirection: "column",
    justifyContent: "center"
  }
};
