export function _textStyle(textColor, fontSize) {
  return {
    fontSize,
    color: textColor
  };
}

export default {
  container: {
    padding: 8,
    marginLeft: 5,
    marginRight: 5,
    alignItems: "center",
    alignContent: "center",
    flexDirection: "column",
    justifyContent: "center"
  }
};
